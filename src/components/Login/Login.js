import React from 'react';
import LoginForm from './LoginForm';
const Login = (props) => {
    return (
        <div>
            <LoginForm {...this.props} />
        </div>
    );
};

export default Login;